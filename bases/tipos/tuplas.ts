(()=>{

    // las tuplas son arreglos con un numero fijo de elementos
    // y con tipos de datos definidos
    const persona: [string, number, boolean] = ['Jorge', 31, true];
    console.log(persona);
    console.log(persona[0]);
    console.log(persona[1]); //podemos acceder a los elementos por indice sin problema


})()