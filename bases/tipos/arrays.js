"use strict";
(() => {
    // Se puede definir un array de la siguiente manera
    const medicos = ['Dr. A', 'Dr. B', 'Dr. C'];
    // Otra forma de definir un array es de la siguiente manera
    // Ambaas formas son correctas
    const medicos2 = ['Dr. A', 'Dr. B', 'Dr. C'];
    console.log(medicos);
    console.log(medicos2);
    // Se accede a los elementos de la forma que ya conocemos
    console.log(medicos[0]);
})();
